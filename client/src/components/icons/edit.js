import React from "react";

import EditIcon from "../../images/icons/pencil.svg";
import Icon from "../icon";

const Edit = props => {
  return (
    <Icon {...props}>
      <EditIcon />
    </Icon>
  );
};

export default Edit;
